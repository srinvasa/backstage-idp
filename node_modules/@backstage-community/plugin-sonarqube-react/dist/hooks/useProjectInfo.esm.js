import { SONARQUBE_PROJECT_KEY_ANNOTATION } from '../components/isSonarQubeAvailable.esm.js';

const SONARQUBE_PROJECT_INSTANCE_SEPARATOR = "/";
const useProjectInfo = (entity) => {
  let projectInstance = void 0;
  let projectKey = void 0;
  const annotation = entity?.metadata.annotations?.[SONARQUBE_PROJECT_KEY_ANNOTATION];
  if (annotation) {
    const instanceSeparatorIndex = annotation.indexOf(
      SONARQUBE_PROJECT_INSTANCE_SEPARATOR
    );
    if (instanceSeparatorIndex > -1) {
      projectInstance = annotation.substring(0, instanceSeparatorIndex);
      projectKey = annotation.substring(instanceSeparatorIndex + 1);
    } else {
      projectKey = annotation;
    }
  }
  return { projectInstance, projectKey };
};

export { SONARQUBE_PROJECT_INSTANCE_SEPARATOR, useProjectInfo };
//# sourceMappingURL=useProjectInfo.esm.js.map
