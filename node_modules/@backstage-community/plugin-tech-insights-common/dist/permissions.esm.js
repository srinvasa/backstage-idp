import { createPermission } from '@backstage/plugin-permission-common';

const techInsightsCheckReadPermission = createPermission({
  name: "tech-insights.check.read",
  attributes: {
    action: "read"
  }
});
const techInsightsCheckUpdatePermission = createPermission({
  name: "tech-insights.check.run",
  attributes: {
    action: "update"
  }
});
const techInsightsFactRetrieverReadPermission = createPermission({
  name: "tech-insights.fact-retriever.read",
  attributes: {
    action: "read"
  }
});
const techInsightsPermissions = [
  techInsightsCheckReadPermission,
  techInsightsCheckUpdatePermission,
  techInsightsFactRetrieverReadPermission
];

export { techInsightsCheckReadPermission, techInsightsCheckUpdatePermission, techInsightsFactRetrieverReadPermission, techInsightsPermissions };
//# sourceMappingURL=permissions.esm.js.map
