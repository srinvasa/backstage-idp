import { jsxs, jsx } from 'react/jsx-runtime';
import { useState } from 'react';
import Tabs from '@material-ui/core/Tabs';
import Tab from '@material-ui/core/Tab';
import { InfoCard } from '@backstage/core-components';

const ComponentTabs = (props) => {
  const { title, tabs } = props;
  const [value, setValue] = useState(0);
  const handleChange = (_event, newValue) => {
    setValue(newValue);
  };
  return /* @__PURE__ */ jsxs(InfoCard, { title, children: [
    /* @__PURE__ */ jsx(Tabs, { value, onChange: handleChange, children: tabs.map((t) => /* @__PURE__ */ jsx(Tab, { label: t.label }, t.label)) }),
    tabs.map(({ Component }, idx) => /* @__PURE__ */ jsx(
      "div",
      {
        ...idx !== value ? { style: { display: "none" } } : {},
        children: /* @__PURE__ */ jsx(Component, {})
      },
      idx
    ))
  ] });
};

export { ComponentTabs };
//# sourceMappingURL=ComponentTabs.esm.js.map
