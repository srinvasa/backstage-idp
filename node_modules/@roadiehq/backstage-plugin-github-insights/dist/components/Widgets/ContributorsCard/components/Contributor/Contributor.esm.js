import { jsx } from 'react/jsx-runtime';
import { Tooltip, Avatar } from '@material-ui/core';
import { withStyles } from '@material-ui/core/styles';
import ContributorTooltipContent from '../ContributorTooltipContent/ContributorTooltipContent.esm.js';

const LightTooltip = withStyles({
  tooltip: {
    backgroundColor: "white",
    border: "1px solid lightgrey",
    color: "#333",
    minWidth: "320px"
  }
})(Tooltip);
const Contributor = ({ contributor }) => /* @__PURE__ */ jsx(
  LightTooltip,
  {
    title: /* @__PURE__ */ jsx(ContributorTooltipContent, { contributorLogin: contributor.login }),
    interactive: true,
    children: /* @__PURE__ */ jsx(
      Avatar,
      {
        alt: contributor.login,
        src: contributor.avatar_url
      },
      contributor.login
    )
  }
);

export { Contributor as default };
//# sourceMappingURL=Contributor.esm.js.map
