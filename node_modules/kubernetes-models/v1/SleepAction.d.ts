import { ModelData, Model } from "@kubernetes-models/base";
/**
 * SleepAction describes a "sleep" action.
 */
export interface ISleepAction {
    /**
     * Seconds is the number of seconds to sleep.
     */
    "seconds": number;
}
/**
 * SleepAction describes a "sleep" action.
 */
export declare class SleepAction extends Model<ISleepAction> implements ISleepAction {
    "seconds": number;
    constructor(data?: ModelData<ISleepAction>);
}
export { ISleepAction as IIoK8sApiCoreV1SleepAction, SleepAction as IoK8sApiCoreV1SleepAction };
